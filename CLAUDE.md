# 要約

このファイルは Claude Code での開発ルールの要約です。詳細なルールは [`AGENTS.md`](./AGENTS.md) に記載されています。

## 主要なルール

**言語使い分け：**
- すべての出力（チャット、コード、ドキュメント、PR、コミット）は日本語
- 「変数名・関数名・クラス名・ファイル名・ブランチ名・技術用語・コマンド名・URL は英語のまま」

**Git 作業フロー：**
- `main` ブランチへの直接コミット・プッシュは禁止
- 作業ブランチは `feature/`、`fix/`、`docs/`、`refactor/`、`test/` の接頭辞で命名
- コミットメッセージは `feat:`、`fix:`、`docs:` などのプレフィックス + 日本語本文

**セキュリティ：**
- 機密情報（`.env` ファイルや認証情報）をコミットしない
- ユーザーの許可なく `git config` を変更したり、`--no-verify` などでフックをスキップしない

**その他：**
- PR タイトル・本文・レビュー返信は日本語で対応
- 詳細ルールは [`AGENTS.md`](./AGENTS.md) を参照
