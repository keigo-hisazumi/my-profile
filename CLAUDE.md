# Claude 向け指示書

このリポジトリで Claude が作業する際は、以下のルールを必ず守ってください。

## プルリクエスト (PR) は日本語で記載すること

Claude が作成するすべての PR は、**タイトル・本文ともに日本語で記載してください**。これは例外なく適用されます。

### 必須事項

- PR タイトルは日本語で記述する
- PR 本文（Summary / Test plan などのセクションを含む）は日本語で記述する
- セクション見出し（例: `## 概要`, `## 変更点`, `## テスト計画`）も日本語にする
- レビューコメントへの返信も日本語で行う

### PR テンプレート（推奨）

```markdown
## 概要
<このPRで何を、なぜ変更したかを1〜3個の箇条書きで>

## 変更点
- <主要な変更点>

## テスト計画
- [ ] <検証項目>
```

### 英語のまま使用してよいもの

以下は日本語化せず、そのまま英語で記述してください:

- 変数名・関数名・クラス名・ファイル名・ブランチ名
- 技術用語（API、HTTP、JSON、TypeScript など）
- 外部ライブラリ名・npm パッケージ名・コマンド名
- URL、ファイルパス、コードブロック内のコード
- コミットメッセージのプレフィックス（`feat:`, `fix:`, `docs:` など）

## コミットメッセージ

コミットメッセージの本文も日本語で記述してください。プレフィックスは英語のまま使用します。

例:
```
feat: ユーザー一覧画面を追加

- ユーザー検索機能を実装
- ページネーション機能を追加
```

## コード内コメント・ドキュメント

コード内のコメント、README、その他ドキュメントも原則として日本語で記述してください。詳細は `.github/copilot-instructions.md` のルールに準拠します。
